import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.lingganshouji',
  appName: 'lingganshouji',
  webDir: 'dist'
};

export default config;
